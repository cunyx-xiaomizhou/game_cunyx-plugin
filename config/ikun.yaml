#请严格按照yaml格式的文件修改参数，否则报错请备份/data，/config和/resources文件夹后重装本插件，你问我怎么备份？保存到其他文件夹，重装完再覆盖呗~真的是~
#如果你不按照下方要求填写参数，报错了我可管不到，我的插件在发布之前都测试过的，找群有测试过bug的。
#以下参数和冒号之间都有一个空格
#以下参数都没有引号！
#都！没！有！引！号！
#年份信息可为负数，其他的若为负数则按照默认值处理！
#因为最近在搞公元前的这个系统，只有年份做了适配，其他的真不建议你改成0以下的。

#下一行是最早出生日期(年)，不可为空，默认值1950
nian_low: 1950
#下一行是最晚出生日期(年)，不可为空，默认值今年年份(2023)
nian_height: 2023
#下一行是最早出生日期(月)，不可为空，默认值1
yue_low: 1
#下一行是最晚出生日期(月)，不可为空，默认值12
yue_height: 12
#下一行是最早出生日期(日)，不可为空，默认值1
ri_low: 1
#后一行是最晚出生日期(日)，不可为空，默认值28
#为什么下一行默认值是28，我怕出现十月三十一号、二月三十号这类的
ri_height: 28
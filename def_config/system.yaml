#一些系统回复的文案
#这个交给开发人员吧，你们就不用管了~

#参数列表：
# def -- 默认数据
# start -- 初始化回复文案，支持函数{_path_}
#函数列表
# {_path_} -- 返回初始化路径(例如:data/all/786034611.json)
def:
  start:
    - "没有数据，已经初始化到{_path_}"
786034611:
  start:
    - "没有数据，已经初始化到{_path_}"